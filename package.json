{
  "name": "utpay-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "mobile": "bun --filter mobile start",
    "api": "bun --filter api dev",
    "dev": "bun --filter \"*\" dev",
    "install:all": "bun install"
  }
}
